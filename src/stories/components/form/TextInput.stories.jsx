import React from 'react';

import FormText from './TextInput';


export default {
  title: 'Components/Form/Text',
  component: FormText,
};

const Template = (args) => <FormText {...args} />;

export const Text = Template.bind({});
